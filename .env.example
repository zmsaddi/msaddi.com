# Environment Variables Template for MSADDI.EST
# =============================================================================
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local for local development
# 2. Replace all placeholder values with your actual API keys
# 3. For production deployment, add all variables to Vercel Dashboard
#    → See ../VERCEL_ENV_SETUP.md for detailed Vercel setup instructions
# =============================================================================

# ⚠️ SECURITY WARNING:
# - NEVER commit .env.local to version control (it's already in .gitignore)
# - NEVER hardcode API keys in source code
# - Production values MUST be set in Vercel Dashboard, NOT in code

# =============================================================================
# EMAIL CONFIGURATION (Required)
# =============================================================================
# Resend API Key - Get from: https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here

# Email addresses for contact form
EMAIL_FROM=noreply@yourdomain.com
EMAIL_TO=info@yourdomain.com

# =============================================================================
# GOOGLE reCAPTCHA v3 (Required)
# =============================================================================
# Get your keys from: https://www.google.com/recaptcha/admin
# Site Key (public - exposed in client-side code)
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# Secret Key (private - server-only, NEVER expose to client)
RECAPTCHA_SECRET_KEY=your_recaptcha_secret_key_here

# =============================================================================
# GOOGLE ANALYTICS (Optional)
# =============================================================================
# Get your measurement ID from: https://analytics.google.com
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# =============================================================================
# VERCEL KV (Redis) - RATE LIMITING (Required for Production)
# =============================================================================
# Vercel KV provides distributed rate limiting across serverless functions
# Get from: Vercel Dashboard → Storage → Create KV Database
# These variables are automatically injected by Vercel when KV is connected
# For local development, leave empty (rate limiter will fallback gracefully)

# KV_URL=your_kv_rest_url_here
# KV_REST_API_URL=your_kv_rest_api_url_here
# KV_REST_API_TOKEN=your_kv_rest_api_token_here
# KV_REST_API_READ_ONLY_TOKEN=your_kv_readonly_token_here

# =============================================================================
# SITE CONFIGURATION
# =============================================================================
# Your production website URL (used for SEO, sitemap, canonical URLs)
NEXT_PUBLIC_SITE_URL=https://www.yourdomain.com

# =============================================================================
# DEPLOYMENT CHECKLIST:
# =============================================================================
# Local Development:
#   ✓ Copy this file to .env.local
#   ✓ Fill in all values with your actual API keys
#   ✓ Run: npm run dev
#
# Production (Vercel):
#   ✓ Go to Vercel Dashboard → Your Project → Settings → Environment Variables
#   ✓ Add all required variables above (see ../VERCEL_ENV_SETUP.md for exact values)
#   ✓ Select: Production + Preview + Development for each variable
#   ✓ Go to Storage → Create KV Database → Connect to your project (auto-injects KV variables)
#   ✓ Deploy or redeploy your project